apiVersion: v1
kind: Service
metadata:
  name: claude-code-proxy
  labels:
    app.kubernetes.io/name: claude-code-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claude-code-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
